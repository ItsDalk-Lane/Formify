# MVP #025: Tool Calling 框架

## 状态: pending

## 目标
实现 AI Chat 的工具调用框架。

## 功能边界
- [x] ToolRegistryService（工具注册）
- [x] ToolExecutionManager（执行管理）
- [x] 内置工具：ReadFile, WriteFile, SearchFiles
- [x] 工具审批机制
- [x] 工具结果显示

## 不做
- 所有内置工具
- 自定义工具定义 UI

## 验收标准
1. 工具能正确注册
2. AI 能调用工具
3. 工具执行结果能返回给 AI

## 依赖
- MVP023（Chat 基础）

## 参考代码
```typescript
// src/features/chat/services/ToolRegistryService.ts
export class ToolRegistryService {
  private tools: Map<string, Tool> = new Map();

  register(tool: Tool): void {
    this.tools.set(tool.name, tool);
  }

  getTool(name: string): Tool | undefined {
    return this.tools.get(name);
  }

  getToolDefinitions(): ToolDefinition[] {
    return Array.from(this.tools.values()).map(t => ({
      type: 'function',
      function: {
        name: t.name,
        description: t.description,
        parameters: t.parameters
      }
    }));
  }
}
```

## 预估时间
150-180 分钟
