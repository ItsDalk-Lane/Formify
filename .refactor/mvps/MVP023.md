# MVP #023: Chat 基础功能

## 状态: pending

## 目标
实现 AI Chat 的基础对话功能。

## 功能边界
- [x] ChatFeatureManager 类
- [x] ChatService（会话管理）
- [x] ChatView（侧边栏视图）
- [x] ChatMessages 组件
- [x] ChatInput 组件
- [x] 基础命令注册

## 不做
- 历史记录
- Tool Calling
- Selection Toolbar

## 验收标准
1. 能打开 Chat 侧边栏
2. 能发送消息并获取回复
3. 消息能正确显示

## 依赖
- MVP020（Provider 框架）
- MVP021（OpenAI Provider）

## 参考代码
```typescript
// src/features/chat/ChatFeatureManager.tsx
export class ChatFeatureManager {
  constructor(private plugin: FormPlugin) {}

  initialize(): void {
    this.plugin.registerView(
      CHAT_VIEW_TYPE,
      (leaf) => new ChatView(leaf, this.plugin)
    );

    this.plugin.addCommand({
      id: 'form-chat-open-default',
      name: 'Open AI Chat',
      callback: () => this.openChat()
    });
  }

  async openChat(): Promise<void> {
    const leaf = this.plugin.app.workspace.getRightLeaf(false);
    await leaf?.setViewState({ type: CHAT_VIEW_TYPE, active: true });
  }
}
```

## 预估时间
180-240 分钟
